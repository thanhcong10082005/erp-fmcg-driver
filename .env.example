# Driver App — Environment Variables
# Copy file này thành .env rồi điền giá trị thực tế

# ── API Backend ──────────────────────────────────────────────
# URL của backend NestJS (CÓ THỂ có hoặc không có /api ở cuối —
# apiClient sẽ tự động normalize)
VITE_API_URL=https://erp-fmcg-backend.onrender.com
